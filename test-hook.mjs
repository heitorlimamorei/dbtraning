import { renderHook } from '@testing-library/react-hooks';
import { useChat } from '@ai-sdk/react';
// I can't easily run renderHook without installing it.
// I will just read the dist files of @ai-sdk/react.
